name: PR Validation

on:
  pull_request:
    branches: [develop, staging, main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "1"
  ServiceDefaultsVersion: 1.0.81-alpha
  MessagingContractsVersion: 1.0.91-alpha

jobs:
  dependency-packages:
    name: Build exact public-source dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      artifact-digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Checkout PaymentService source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Test Artifact Registry promotion decisions
        run: bash scripts/tests/promote-artifact-image.test.sh

      - name: Checkout exact MessagingContracts source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: MALIEV-Co-Ltd/Maliev.MessagingContracts
          ref: 0bcd4c704d842211c5ff9bd6b9c4b3aacfcbd8e7
          path: .ci-sources/Maliev.MessagingContracts
          persist-credentials: false

      - name: Checkout exact ServiceDefaults source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: MALIEV-Co-Ltd/Maliev.Aspire
          ref: 7121d57705fc1eff6c7ebb6a69e33e9c26ebfccc
          path: .ci-sources/Maliev.Aspire
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          dotnet-version: 10.0.x

      - name: Build exact dependency packages
        run: bash scripts/prepare-payment-ci-packages.sh

      - name: Upload exact dependency packages
        id: upload
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: payment-ci-packages-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-packages/*
          if-no-files-found: error
          include-hidden-files: true
          overwrite: true
          retention-days: 1

  build-and-test:
    name: Release build, test, and audit
    needs: dependency-packages
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          dotnet-version: 10.0.x

      - name: Download exact dependency packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: payment-ci-packages-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-packages

      - name: Verify dependency artifact integrity
        shell: bash
        env:
          EXPECTED_ARTIFACT_DIGEST: ${{ needs.dependency-packages.outputs.artifact-digest }}
        run: |
          set -euo pipefail
          test -n "$EXPECTED_ARTIFACT_DIGEST"
          printf 'GitHub artifact digest: %s\n' "$EXPECTED_ARTIFACT_DIGEST"
          (cd .ci-packages && sha256sum --check SHA256SUMS)

      - name: Restore exact package graph
        run: dotnet restore Maliev.PaymentService.slnx --configfile NuGet.PRValidation.Config /p:GITHUB_ACTIONS=true /p:ServiceDefaultsVersion="1.0.81-alpha" /p:MessagingContractsVersion="1.0.91-alpha"

      - name: Build
        run: dotnet build Maliev.PaymentService.slnx --configuration Release --no-restore /p:GITHUB_ACTIONS=true /p:ServiceDefaultsVersion="1.0.81-alpha" /p:MessagingContractsVersion="1.0.91-alpha"

      - name: Test
        run: dotnet test Maliev.PaymentService.slnx --configuration Release --no-build --no-restore --verbosity normal /p:GITHUB_ACTIONS=true /p:ServiceDefaultsVersion="1.0.81-alpha" /p:MessagingContractsVersion="1.0.91-alpha"

      - name: Audit resolved NuGet graph
        run: dotnet list Maliev.PaymentService.slnx package --include-transitive --vulnerable --no-restore --configfile NuGet.PRValidation.Config

  container:
    name: Build and scan production image
    needs: dependency-packages
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      IMAGE_TAG: maliev-paymentservice:pr-validation-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Checkout source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Download exact dependency packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: payment-ci-packages-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-packages

      - name: Verify dependency artifact integrity
        shell: bash
        env:
          EXPECTED_ARTIFACT_DIGEST: ${{ needs.dependency-packages.outputs.artifact-digest }}
        run: |
          set -euo pipefail
          test -n "$EXPECTED_ARTIFACT_DIGEST"
          printf 'GitHub artifact digest: %s\n' "$EXPECTED_ARTIFACT_DIGEST"
          (cd .ci-packages && sha256sum --check SHA256SUMS)

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Build local production image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Maliev.PaymentService.Api/Dockerfile
          load: true
          push: false
          tags: ${{ env.IMAGE_TAG }}
          build-args: |
            dependency_restore_stage=restore-local
            SERVICE_DEFAULTS_VERSION=${{ env.ServiceDefaultsVersion }}
            MESSAGING_CONTRACTS_VERSION=${{ env.MessagingContractsVersion }}

      - name: Validate runtime image metadata
        shell: bash
        run: |
          set -euo pipefail
          metadata="$(docker image inspect "$IMAGE_TAG")"
          test "$(jq -r '.[0].Config.User' <<< "$metadata")" = "app"
          test "$(jq -c '.[0].Config.Entrypoint' <<< "$metadata")" = '["dotnet","Maliev.PaymentService.Api.dll"]'
          test "$(jq -r '.[0].Config.ExposedPorts | has("8080/tcp")' <<< "$metadata")" = "true"

      - name: Smoke test production image liveness
        shell: bash
        run: |
          set -euo pipefail
          network_name="payment-smoke-net-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          database_name="payment-smoke-db-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          application_name="payment-smoke-app-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          cleanup() {
            docker rm --force "$application_name" "$database_name" >/dev/null 2>&1 || true
            docker network rm "$network_name" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          docker network create "$network_name" >/dev/null
          docker run --detach \
            --name "$database_name" \
            --network "$network_name" \
            --env POSTGRES_PASSWORD=payment-smoke-password \
            --env POSTGRES_DB=PaymentDb \
            postgres:18-alpine >/dev/null

          for attempt in {1..30}; do
            if docker exec "$database_name" pg_isready --username postgres --dbname PaymentDb >/dev/null 2>&1; then
              break
            fi
            if [[ "$attempt" == "30" ]]; then
              docker logs "$database_name"
              exit 1
            fi
            sleep 1
          done

          docker run --detach \
            --name "$application_name" \
            --network "$network_name" \
            --publish "127.0.0.1:18080:8080" \
            --env ASPNETCORE_ENVIRONMENT=Testing \
            --env "ConnectionStrings__PaymentDbContext=Host=$database_name;Port=5432;Database=PaymentDb;Username=postgres;Password=payment-smoke-password" \
            --env "ConnectionStrings__redis=payment-smoke-redis:6379" \
            --env "ConnectionStrings__rabbitmq=amqp://guest:guest@payment-smoke-rabbitmq:5672" \
            --env "CORS__AllowedOrigins__0=http://localhost:3000" \
            --env "Features__FailOpenOnIAMError=true" \
            --env "IAM__RegistrationDelaySeconds=0" \
            "$IMAGE_TAG" >/dev/null

          for attempt in {1..60}; do
            if curl --fail --silent "http://127.0.0.1:18080/payment/liveness" >/dev/null; then
              exit 0
            fi
            if [[ "$(docker inspect --format '{{.State.Running}}' "$application_name")" != "true" ]]; then
              docker logs "$application_name"
              exit 1
            fi
            sleep 1
          done

          docker logs "$application_name"
          exit 1

      - name: Generate image SBOM
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.IMAGE_TAG }}
          format: cyclonedx
          output: trivy-sbom.cdx.json
          scanners: vuln
          exit-code: "0"

      - name: Validate SBOM output
        run: test -s trivy-sbom.cdx.json

      - name: Scan image for high-severity vulnerabilities
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.IMAGE_TAG }}
          format: table
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "1"

  protected-branch-gate:
    name: validate
    needs: [dependency-packages, build-and-test, container]
    uses: ./.github/workflows/_protected-branch-gate.yml
